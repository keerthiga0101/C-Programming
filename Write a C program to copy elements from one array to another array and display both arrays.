#include <stdio.h>

int main() {
    int a[3] = {1,2,3}, b[3], i;

    for(i = 0; i < 3; i++)
        b[i] = a[i];

    printf("Copied array:\n");
    for(i = 0; i < 3; i++)
        printf("%d ", b[i]);

    return 0;
}