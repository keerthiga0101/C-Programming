#include <stdio.h>

int main() {
    int a[5] = {10,20,30}, n = 3, x;

    printf("Enter new element: ");
    scanf("%d", &x);

    a[n] = x;
    n++;

    for(int i = 0; i < n; i++)
        printf("%d ", a[i]);

    return 0;
}