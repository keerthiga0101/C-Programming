#include <stdio.h>

float interest(float p, float r, int t) {
    return (p * r * t) / 100;
}

int main() {
    float p, r;
    int t;

    printf("Enter principal, rate, time: ");
    scanf("%f%f%d", &p, &r, &t);

    printf("Interest = %.2f", interest(p, r, t));
    return 0;
}