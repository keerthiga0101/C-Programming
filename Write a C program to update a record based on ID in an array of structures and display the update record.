#include <stdio.h>

struct emp {
    int id;
    char name[20];
};

int main() {
    struct emp e[1] = {1,"Ravi"};

    scanf("%s", e[0].name);
    printf("Updated Name: %s", e[0].name);

    return 0;
}