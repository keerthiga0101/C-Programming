#include <stdio.h>

int main() {
    float price;
    int quantity;
    float total;

    printf("Enter price of item: ");
    scanf("%f", &price);

    printf("Enter quantity: ");
    scanf("%d", &quantity);

    total = price * quantity;

    printf("Total bill amount = %.2f\n", total);
    return 0;
}
