#include <stdio.h>

int main() {
    int sales[5], i, total = 0;

    for(i = 0; i < 5; i++) {
        scanf("%d", &sales[i]);
        total += sales[i];
    }

    printf("Total sales = %d", total);
    return 0;
}